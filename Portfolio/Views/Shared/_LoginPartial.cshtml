@using Microsoft.AspNetCore.Identity
@inject SignInManager<Portfolio.Data.User> SignInManager
@inject UserManager<Portfolio.Data.User> UserManager
<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        @*<li class="nav-item">
            <a class="nav-link text-light" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name</a>
        </li>*@
        <li class="nav-item text-light">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("newhome", "DefaultHome", new { area = "" })">
                <button id="LogoutBtn" type="submit" class="btn btn-outline-danger" style="margin-top:11%;">Logout</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="btn btn-sm" style="color:#a92330!important" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
        <li class="nav-item">
            <a class="btn btn-sm" style="color:#a92330!important" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
        
    }
</ul>
